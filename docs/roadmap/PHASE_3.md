# Phase 3: 账户管理

> 版本: v1.3.0 | 状态: 📋 待开发 | 依赖: Phase 1

## 目标

提供账户设置界面，允许用户管理个人资料、配置自定义 Gemini API Key、以及删除账户。

## 核心功能

- [ ] 账户设置页面
- [ ] 个人资料编辑 (名称/头像)
- [ ] 自定义 Gemini API Key 配置
- [ ] API Key 验证
- [ ] 账户删除

## 前置条件

- ✅ Phase 1 完成 (用户认证系统)
- ✅ user_profiles 表已创建

## 任务清单

### 任务 3.1: AccountSettings 组件
**状态**: 📋 待开发

**目标**: 创建账户设置页面主组件

**文件**: `src/components/auth/AccountSettings.tsx`

**Props**:
```typescript
interface AccountSettingsProps {
  isOpen: boolean;
  onClose: () => void;
}
```

**UI 设计**:
```
┌─────────────────────────────────────────────────────────┐
│  ←  账户设置                                            │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │  👤 个人资料                                    │   │
│  │                                                 │   │
│  │     ┌───────┐                                   │   │
│  │     │ 头像  │  ← 点击更换                       │   │
│  │     └───────┘                                   │   │
│  │                                                 │   │
│  │  显示名称                                       │   │
│  │  ┌─────────────────────────────────────────┐   │   │
│  │  │  小明                                   │   │   │
│  │  └─────────────────────────────────────────┘   │   │
│  │                                                 │   │
│  │  邮箱 (只读)                                    │   │
│  │  xiaoming@gmail.com                            │   │
│  │                                                 │   │
│  │  ┌─────────────────┐                           │   │
│  │  │   保存修改      │                           │   │
│  │  └─────────────────┘                           │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │  🔑 API 设置                                    │   │
│  │                                                 │   │
│  │  当前状态: ✅ 使用自定义 Key                    │   │
│  │                                                 │   │
│  │  Gemini API Key                                 │   │
│  │  ┌─────────────────────────────────────────┐   │   │
│  │  │  AIza••••••••••••••••                   │   │   │
│  │  └─────────────────────────────────────────┘   │   │
│  │                                                 │   │
│  │  ┌───────────┐  ┌───────────┐                  │   │
│  │  │ 验证 Key  │  │   保存    │                  │   │
│  │  └───────────┘  └───────────┘                  │   │
│  │                                                 │   │
│  │  💡 添加自己的 Key 可无限使用 Magic Edit       │   │
│  │  获取: https://aistudio.google.com/apikey      │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │  ⚠️ 危险操作                                   │   │
│  │                                                 │   │
│  │  ┌─────────────────────────────────────────┐   │   │
│  │  │   🗑️ 删除账户                           │   │   │
│  │  └─────────────────────────────────────────┘   │   │
│  │                                                 │   │
│  │  删除账户将清除所有数据，此操作不可恢复。       │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

**布局结构**:
- 全屏覆盖层模式
- 分组卡片布局
- 响应式设计

**验收标准**:
- [ ] 页面正确渲染
- [ ] 分组清晰
- [ ] 响应式布局

---

### 任务 3.2: 个人资料编辑功能
**状态**: 📋 待开发

**目标**: 实现显示名称和头像编辑

**实现内容**:

1. **显示名称编辑**
```tsx
const [displayName, setDisplayName] = useState(user.displayName || '');

const handleSaveProfile = async () => {
  await updateProfile({ displayName });
  // 显示成功提示
};
```

2. **头像更换**
```tsx
// 方案 A: URL 输入
const [avatarUrl, setAvatarUrl] = useState(user.avatarUrl || '');

// 方案 B: 文件上传 (需要 Supabase Storage)
const handleFileUpload = async (file: File) => {
  const url = await uploadAvatar(file);
  setAvatarUrl(url);
};
```

**建议**: 初期使用 URL 输入方案，简化实现

**验收标准**:
- [ ] 显示名称可编辑
- [ ] 头像可更换
- [ ] 保存成功后更新显示
- [ ] 输入验证 (名称长度等)

---

### 任务 3.3: API Key 配置功能
**状态**: 📋 待开发

**目标**: 允许用户配置自定义 Gemini API Key

**UI 组件**: `ApiKeySettings` (可内嵌在 AccountSettings)

**功能流程**:
```
1. 用户输入 API Key
2. 点击"验证 Key"
3. 调用 /api/validate-key 验证
4. 验证通过 → 显示 ✅，启用保存按钮
5. 验证失败 → 显示错误提示
6. 点击保存 → 更新 user_profiles.custom_gemini_key
```

**状态显示**:
```tsx
// 无自定义 Key
<div className="text-gray-500">
  当前状态: 使用系统 Key (每日限额 3 次)
</div>

// 有自定义 Key
<div className="text-green-500">
  当前状态: ✅ 使用自定义 Key (无限制)
</div>
```

**安全考虑**:
- Key 显示为部分遮盖: `AIza••••••••`
- 可选择显示/隐藏完整 Key
- 提供删除 Key 功能

**验收标准**:
- [ ] Key 输入框
- [ ] 验证功能正常
- [ ] 保存成功
- [ ] 删除 Key 功能
- [ ] 敏感信息遮盖

---

### 任务 3.4: API Key 验证接口
**状态**: 📋 待开发

**目标**: 创建 API Key 验证端点

**文件**: `api/validate-key.js`

**实现**:
```javascript
import { GoogleGenAI } from '@google/genai';

export default async function handler(req, res) {
  if (req.method !== 'POST') {
    return res.status(405).json({ error: 'Method not allowed' });
  }

  const { apiKey } = req.body;

  if (!apiKey || typeof apiKey !== 'string') {
    return res.status(400).json({ valid: false, error: 'API key required' });
  }

  try {
    const ai = new GoogleGenAI({ apiKey });

    // 使用轻量级请求验证 Key
    await ai.models.list();

    return res.status(200).json({ valid: true });
  } catch (error) {
    return res.status(200).json({
      valid: false,
      error: 'Invalid API key'
    });
  }
}
```

**验收标准**:
- [ ] 有效 Key 返回 valid: true
- [ ] 无效 Key 返回 valid: false
- [ ] 错误处理完善

---

### 任务 3.5: 账户删除功能
**状态**: 📋 待开发

**目标**: 实现账户删除流程

**UI 流程**:
```
1. 点击"删除账户"按钮
2. 弹出确认对话框
3. 用户输入 "DELETE" 确认
4. 调用删除 API
5. 清除本地状态
6. 跳转到首页
```

**确认对话框**:
```
┌───────────────────────────────────────────┐
│                                           │
│  ⚠️ 确认删除账户?                         │
│                                           │
│  此操作将永久删除:                         │
│  • 你的所有照片                           │
│  • 你的账户信息                           │
│  • 你分享到公共画廊的照片                  │
│                                           │
│  此操作不可恢复！                          │
│                                           │
│  请输入 "DELETE" 确认:                    │
│  ┌─────────────────────────────────────┐  │
│  │                                     │  │
│  └─────────────────────────────────────┘  │
│                                           │
│  ┌───────────┐  ┌───────────────────┐    │
│  │   取消    │  │  永久删除账户     │    │
│  └───────────┘  └───────────────────┘    │
│                                           │
└───────────────────────────────────────────┘
```

**后端处理**:
- 由于使用 Supabase Auth，可使用 `supabase.auth.admin.deleteUser()`
- 或者让用户自己删除：提供说明页面

**注意**: Supabase 免费版可能不支持完全删除用户
- 备选方案: 标记账户为 "deleted"，停用登录

**验收标准**:
- [ ] 确认对话框显示
- [ ] 需要输入确认文字
- [ ] 删除后清除状态
- [ ] 删除后跳转首页

---

## 文件创建清单

| 文件路径 | 任务 | 说明 |
|----------|------|------|
| `src/components/auth/AccountSettings.tsx` | 3.1 | 设置页面 |
| `api/validate-key.js` | 3.4 | Key 验证接口 |

## 修改文件清单

| 文件路径 | 任务 | 修改内容 |
|----------|------|----------|
| `src/services/authService.ts` | 3.2, 3.3 | 添加 updateProfile |
| `src/components/auth/UserMenu.tsx` | 3.1 | 添加设置入口 |
| `src/constants.ts` | 3.1-3.5 | 添加翻译文本 |

## 翻译文本

```typescript
// constants.ts 新增
const TRANSLATIONS = {
  en: {
    // 账户设置
    accountSettings: 'Account Settings',
    profile: 'Profile',
    displayName: 'Display Name',
    email: 'Email',
    saveChanges: 'Save Changes',

    // API 设置
    apiSettings: 'API Settings',
    currentStatus: 'Current Status',
    usingSystemKey: 'Using system key (3 times/day)',
    usingCustomKey: 'Using custom key (Unlimited)',
    geminiApiKey: 'Gemini API Key',
    validateKey: 'Validate Key',
    keyValid: 'Key is valid',
    keyInvalid: 'Key is invalid',
    getApiKey: 'Get your API key',
    removeKey: 'Remove Key',

    // 危险操作
    dangerZone: 'Danger Zone',
    deleteAccount: 'Delete Account',
    deleteConfirm: 'This will permanently delete all your data. This cannot be undone.',
    typeDelete: 'Type "DELETE" to confirm',
    permanentlyDelete: 'Permanently Delete',
  },
  zh: {
    // 账户设置
    accountSettings: '账户设置',
    profile: '个人资料',
    displayName: '显示名称',
    email: '邮箱',
    saveChanges: '保存修改',

    // API 设置
    apiSettings: 'API 设置',
    currentStatus: '当前状态',
    usingSystemKey: '使用系统 Key (每日 3 次)',
    usingCustomKey: '使用自定义 Key (无限制)',
    geminiApiKey: 'Gemini API Key',
    validateKey: '验证 Key',
    keyValid: 'Key 有效',
    keyInvalid: 'Key 无效',
    getApiKey: '获取 API Key',
    removeKey: '删除 Key',

    // 危险操作
    dangerZone: '危险操作',
    deleteAccount: '删除账户',
    deleteConfirm: '此操作将永久删除所有数据，无法恢复。',
    typeDelete: '输入 "DELETE" 确认',
    permanentlyDelete: '永久删除',
  },
};
```

## 测试用例

### 功能测试
- [ ] 显示名称编辑保存
- [ ] 头像 URL 更换
- [ ] API Key 验证成功
- [ ] API Key 验证失败
- [ ] API Key 保存
- [ ] API Key 删除
- [ ] 账户删除完整流程

### 安全测试
- [ ] Key 部分遮盖显示
- [ ] 删除需要确认
- [ ] RLS 权限验证

## 注意事项

1. **API Key 安全**
   - 前端仅显示遮盖后的 Key
   - 传输时使用 HTTPS
   - 存储时考虑加密

2. **头像上传**
   - 初期可使用 URL 输入
   - 后期可添加 Supabase Storage 支持
   - 考虑图片大小限制

3. **账户删除**
   - 需要级联删除关联数据
   - 确保不影响公共画廊显示
   - 提供明确的确认机制

## 完成标准

Phase 3 完成的标志：
- [ ] 可以修改显示名称
- [ ] 可以更换头像
- [ ] 可以配置自定义 API Key
- [ ] Key 验证功能正常
- [ ] 可以删除账户
